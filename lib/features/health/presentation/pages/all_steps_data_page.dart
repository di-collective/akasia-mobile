import 'package:flutter/material.dart';

class AllStepsDataPage extends StatelessWidget {
  const AllStepsDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('All Steps Data Page'),
      ),
    );
  }
}
