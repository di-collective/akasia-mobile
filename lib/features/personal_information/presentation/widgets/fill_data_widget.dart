import 'package:flutter/material.dart';

class FillDataWidget extends StatelessWidget {
  const FillDataWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
